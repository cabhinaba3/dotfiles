#!/usr/bin/env bash
# ==============================================================================
# Legacy entry point for backwards compatibility.
# Sources the modular interactive bash configuration.
# ==============================================================================
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -f "$SCRIPT_DIR/interactive.bash" ]; then
    # shellcheck source=bash/interactive.bash
    . "$SCRIPT_DIR/interactive.bash"
fi
unset SCRIPT_DIR
