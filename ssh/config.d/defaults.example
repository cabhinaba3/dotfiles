# Generic, safe-by-default SSH client settings. No secrets, no host names.
# Copy this block to the top of ~/.ssh/config if you want it.

Host *
    ServerAliveInterval 60
    ServerAliveCountMax 3
    AddKeysToAgent yes
    HashKnownHosts yes
